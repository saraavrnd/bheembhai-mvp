# Design Sync — {{story_key}}: {{story_title}}

**Date:** {{date}} · **Branch:** `feat/{{story_key}}-{{slug}}` · **Classification:** {{noop_incremental_structural}}

## What this story changed (vs current design)
<!-- endpoints / data fields / module behavior / integrations / config -->

## Reference updates committed to this branch
<!-- Only files that actually changed. "none" if no-op. -->
- [ ] `architecture.md` —
- [ ] `data-model.md` —
- [ ] `api-contracts/` —
- [ ] `AGENTS.md` —
- [ ] `docs/CHANGELOG-design.md` — entry appended

## Affected in-progress stories (flagged for design re-check)
<!-- From Atlassian MCP: stories In Progress / In Review touching the same module/endpoint/entity.
     Each gets a Jira comment recommending a story-design re-check. -->
| Story | Overlap (module / endpoint / entity) | Action |
|-------|--------------------------------------|--------|
| | | commented: re-check story-design |

## Structural-change check
<!-- If this story made a structural change that did NOT go through a tech-design amendment,
     flag it here and recommend pausing to amend tech-design + add an ADR. Usually "none". -->
None — change was incremental / within the existing architecture.

---
*Docs committed to the branch; `pr-create` proceeds to merge them atomically with the code.*
